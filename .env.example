PUBLIC_URL=https://your-domain.com
SOUNDCLOUD_REDIRECT_URI=https://your-domain.com/soundcloud/callback
PORT=3000
# Optional: primary accent color of the login UI (hex, format: #rrggbb).
# Defaults to #3b82f6 (blue) so the page does not visually mimic SoundCloud.
# UI_BRAND_COLOR=#3b82f6
